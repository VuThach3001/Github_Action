name: Events Demo 1
on: 
  pull_request:
    types:
      - opened
  workflow_dispatch:
  push:
    branches:
      - main
      - test
      - 'feature/*'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output event data
        run: echo "${{ toJSON(github.event) }}"
      - name: Get code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
        working-directory: example/section-4
      - name: Test code
        run: npm test
        working-directory: example/section-4
      - name: Build code
        run: npm run build
        working-directory: example/section-4
      - name: Deploy project
        run: echo "Deploying project..."